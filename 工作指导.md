# 工作指导

新 agent 接手这个目录时，先读根目录的 `任务综述.md`，它是当前任务背景和研究目标的入口。

## 目录约定

- `resource/autogen_prefix_tree/` 是前缀重排插件主代码。源码修改默认只能发生在这里。
- `autogen/` 是 AutoGen 源码工具区，默认只读取和调用，不直接修改。
- `cache_hit_proxy/` 是辅助实验插件工具区，默认只读取和调用，不直接修改。
- `config/` 只放本地 DS API、密钥和私有配置，不提交，不复制到报告里。
- `datasets/` 统一存放测试集。当前 `datasets/HumanEval/` 是 HumanEval 测试集，后续测试集也放这里。
- `experiments/` 统一存放实验目录、运行输出、临时文件、报告草稿和历史归档。

## 实验命名

新实验必须在 `experiments/` 下新建单独目录。目录名用一句话或几个词说明实验目的，优先用英文、小写和短横线，例如：

- `groupchat-cache-ab`
- `humaneval-baseline-smoke`
- `deepseek-prefix-sensitivity`

每个实验目录里建议放一个简短 `README.md`，写清楚实验目的、入口命令、输入数据和主要输出。

## 修改规则

- 修改插件能力、测试或文档时，优先只动 `resource/autogen_prefix_tree/`、`tests/` 和根目录必要文档。
- 不要把实验输出写到根目录；输出应放到对应的 `experiments/<experiment-name>/`。
- 不要把测试集、工具源码、配置文件和实验产物混在一起。
- 如果确实需要修改 `autogen/` 或 `cache_hit_proxy/`，必须在实验记录里说明原因和影响范围。
- 提交前运行 `pytest -q`，并确认 `git status --short` 只包含本次任务有意提交的内容。
