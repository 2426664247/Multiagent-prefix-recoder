# 面向 Agent Team 的语义约束前缀化缓存优化

## Background

在 Agent Team / Multi\-Agent 系统中，不同 Agent 的 prompt 通常由多类上下文块组成，包括团队共享知识、任务背景、成员角色约束、工具能力描述、通信协议、历史交互状态以及当前子任务指令等。由于多个 Agent 往往围绕同一任务协同工作，它们的 prompt 中存在大量语义重复或完全重复的上下文内容，例如全局任务目标、共享环境信息、公共工具说明和团队协作规范。

这些跨 Agent 共享内容理论上具有被复用的潜力，可用于降低推理成本和首 token 延迟。然而，当前主流商业 LLM API 的 prompt caching 机制通常只能复用位于 prompt 前缀且完全一致的 token 序列。换言之，只有当共享内容以相同顺序出现在多个请求的开头位置时，才能稳定获得缓存收益。

在实际 Agent Team 编排中，共享上下文并不总是天然位于一致前缀。不同 Agent 通常会在 prompt 开头放置各自的角色定义、工具权限、执行策略或局部状态，导致虽然 prompt 中存在大量重复内容，但这些重复内容分布在不同位置，无法被商业 API 的 prefix\-based prompt caching 直接利用。因此，Agent Team 中存在一种典型错配：语义上可共享的上下文，并不等价于系统层可缓存的前缀。

## Existing Work

现有缓存利用研究主要集中在 LLM serving system 层，目标是在给定请求序列的情况下提升 KV cache 的复用效率和推理吞吐。例如，RadixAttention / SGLang、vLLM prefix caching 等工作通过组织和匹配共享前缀，实现对相同 prefix 的高效 KV cache 复用。这类方法显著提升了具有前缀共享模式的 workload 的 serving 效率。

此外，也有工作尝试突破严格前缀共享的限制。例如 CacheBlend 面向 RAG 场景，对多个预计算文本块的 KV cache 进行融合，并通过选择性重计算缓解非连续上下文拼接带来的质量损失。这说明 serving\-side 的 KV cache reuse 已开始关注更复杂的上下文复用模式。

然而，上述工作大多仍然从云端 serving 或 RAG chunk 复用角度出发，默认输入 prompt 的组织形式已经确定，主要优化的是**如何在模型服务端复用已有 token / chunk 的 KV cache**。它们通常不解决 Agent Team 编排中的另一个关键问题：如何在端侧或 workflow 层重构多个 Agent 的 prompt，使原本语义共享但位置分散的上下文，被安全地转化为跨 Agent 一致前缀，从而适配商业 API 或 prefix\-cache serving 系统的缓存机制。

因此，现有研究尚未系统回答：在 Multi\-Agent 场景下，哪些共享上下文可以被前置？前置后是否会改变 Agent 行为？如何在缓存收益和任务效用之间进行权衡？

## Challenge

Agent Team 中的共享 context 只有在被组织为跨 Agent 一致前缀时，才最容易转化为实际的 prompt caching 收益。但 prompt 不是任意可交换的字符串序列，而是具有语义结构和行为约束的上下文程序。

不同类型的上下文块具有不同的可前置性。例如，全局任务背景、共享环境状态、公共格式约定等内容通常更适合作为共享前缀；而特定 Agent 的角色身份、工具权限、私有记忆、局部推理状态或子任务目标，则可能需要保留在 Agent\-specific 区域中。如果简单地将所有重复内容都提升到共享前缀，可能造成角色污染、工具边界混淆、指令优先级改变、输出格式偏移，甚至任务完成率下降。

因此，核心挑战不只是检测重复字符串，而是识别**“既具有跨 Agent 共享性，又具有语义可前置性”**的上下文块，并在有限的 prompt transformation 空间中优化缓存收益，同时约束 Agent 行为不发生显著退化。

## Problem Formulation

本研究将 Agent Team 中的缓存优化问题形式化为**语义约束下的跨 Agent 层次化前缀因子分解问题**，即：

给定一个 Agent Team 中多个结构化 prompt 序列，如何在保持 Agent 行为效用不显著下降的前提下，将其中可共享、可前置的上下文块组织为一棵层次化 prompt prefix tree，使不同 Agent 的 prompt 对应树中从根到叶子的路径，从而最大化跨 Agent 的实际 prompt caching 收益。

给定一个由 $m$ 个 Agent 组成的 Agent Team，其原始 prompt 集合为：

$\mathcal{P}=\left\{P_{1}, P_{2}, ..., P_{m}\right\}$

其中，每个 Agent 的 prompt $P_{i}$ 可以表示为一组有类型、有边界的上下文块序列：

$P_{i}=\left[b_{i 1}, b_{i 2}, ..., b_{i n_{i}}\right]$

每个上下文块 $b_{i j}$ 具有对应的语义类型，例如全局任务背景、团队协作规则、角色约束、工具说明、历史交互状态、私有记忆或当前子任务指令等。

不同于仅寻找一个被所有 Agent 共享的统一前缀，本研究允许不同 Agent 之间存在不同粒度的共享结构。具体而言，我们希望通过一个受限的 prompt transformation$T \in \mathcal{T}$，将原始 prompt 集合 $\mathcal{P}$ 重构为一棵层次化前缀树：

$\mathcal{G}=(V, E)$

其中，每个节点 $v \in V$ 表示一个上下文块或若干上下文块的组合；根节点附近的内容表示更多 Agent 共享的上下文，越靠近叶子的内容越偏向特定 Agent 或特定子组。（我感觉这种形式比较好，就像之前讨论的，是一个把agent放置到prefix tree节点的过程）

对于每个 Agent $i$，变换后的 prompt$P_{i}'$ 对应于从根节点 $r^{*}$ 到该 Agent 所属叶子节点 $l_{i}$ 的一条路径：

$P_{i}'=Path_{\mathcal{G}}\left(r, l_{i}\right)$

也可以展开表示为：

$P_{i}'=\left[S_{0}, S_{g_{1}(i)}, S_{g_{2}(i)}, ..., A_{i}\right]$

其中，$S_{0}$ 表示所有 Agent 共享的全局前缀，$S_{g_{k}(i)}$ 表示 Agent 所属某一层级或子组共享的上下文块，$A_{i}$ 表示 Agent\-specific 的私有上下文。

对于任意两个 Agent $i$ 和 $j$，它们能够共享的缓存部分由二者路径的最长公共前缀决定：

$SharedPrefix\left( P_{i}', P_{j}'\right)=LCP\left(Path_{\mathcal{G}}\left(r, l_{i}\right) ,Path_{\mathcal{G}}\left(r, l_{j}\right)\right)$

因此，缓存收益不仅来自所有 Agent 共同共享的全局前缀，也来自部分 Agent 子组之间的局部共享前缀。在该层次化结构下，缓存收益可以被建模为树中各共享节点带来的累计收益：

$CacheBenefit(\mathcal {G})=\sum _{v\in V}len(v)\cdot \max(0,c(v)-1)\cdot \alpha(v)$

其中，$len(v)$ 表示节点 $v$ 所包含上下文的 token 长度，$c(v)$ 表示经过节点 $v$ 的 Agent 请求数量或请求频率，$\alpha(v)$ 表示单位 token 的缓存收益，例如价格节省、延迟降低或吞吐提升。

研究目标是在允许的变换集合$\mathcal{T}$ 中寻找最优 prompt 重构策略：

$\max _{T \in \mathcal{T}} \mathbb{E}\left[CacheBenefit \left(\mathcal{G}_{T}\right)\right]$

约束条件：

$Utility (T(\mathcal{P})) \geq Utility(\mathcal{P})-\epsilon$

$T \text{ preserves semantic constraints}$

其中，$\mathcal{G}_{T}$ 表示由变换 $T$ 生成的 prompt prefix tree，$Utility(\cdot)$ 表示 Agent Team 的整体任务效用，可由任务完成率、角色遵循度、工具选择正确性、输出格式一致性、协作质量和安全约束满足情况等指标衡量；$\epsilon$表示可接受的最大效用退化阈值。

语义约束要求变换 $T$ 不能任意重排所有上下文块，而只能对满足一定条件的上下文块进行前置、合并或共享。例如，全局任务背景、公共环境信息、团队协作协议等通常具有较高的可前置性；而角色身份、私有工具权限、私有记忆、当前子任务指令等通常应保留在 Agent\-specific 区域，以避免角色污染、工具边界混淆或任务目标偏移。

因此，本问题的核心是同时学习或设计：

1\. 共享性判断：哪些上下文块在多个 Agent 之间重复或语义等价；

2\. 可前置性判断：哪些共享上下文块可以安全移动到更靠近根的位置；

3\. 层次化分组结构：哪些 Agent 应共享全局前缀，哪些 Agent 子组应共享局部前缀；

4\. 收益\-效用权衡策略：如何在最大化缓存收益的同时，保证变换后的 Agent Team 行为相对于原始系统满足非劣效约束。

## Research Question 总结

因此，本研究的核心问题是：

在 Agent Team / Multi\-Agent 编排中，如何识别多个 Agent prompt 中具有共享性且可安全前置的上下文块，并通过语义约束下的 prompt transformation，将其转化为跨 Agent 一致前缀，从而最大化实际 prompt caching 收益，同时保证 Agent 行为和任务效用。

## 潜在的解决方案\(先写的一个初步参考，后续还要调整\)

本文设计一个面向 Agent Team 的 semantic prefix caching system。核心思路是：在调用远端 LLM 之前，先在端侧分析多个 Agent prompts，找出可共享且可安全前置的上下文块，并将它们组织成层次化 prefix tree，从而提高 prompt caching 命中率；同时通过轻量验证避免 prompt 重构损害 Agent 行为。系统由三个设计点组成。

### Design 1: Local Prompt Compiler

系统首先使用一个轻量级本地 LLM 作为 prompt compiler，将原始 Agent prompts 转换为结构化的 block\-level semantic IR。

该模块负责识别：

\- prompt 中的 context block 边界；

\- block 的语义类型，例如任务背景、团队规则、角色约束、工具描述、当前指令；

\- block 是否在多个 Agent 间共享或语义等价；

\- block 是否适合被前置，以及可能带来的风险。

它的输出不是最终 prompt，而是后续优化使用的语义中间表示。使用本地小模型可以降低分析成本，并减少对远端 API 的额外调用。

### Design 2: Hierarchical Prefix Planner

第二个模块根据 semantic IR 构建层次化 prefix tree。

不同 Agent 之间的共享关系通常是分层的：所有 Agent 共享全局任务背景，部分 Agent 共享工具规则或阶段目标，而每个 Agent 保留自己的角色和子任务。因此，系统不会只生成一个统一共享前缀，而是将上下文组织为：

1\. 全局共享前缀；

2\. 子组共享前缀；

3\. Agent\-specific 后缀。

Planner 根据共享范围、token 长度、调用频率和前置风险，决定每个 block 放在树中的位置。高共享、低风险的内容被放到更靠近根的位置；高风险或个体化内容则保留在叶子附近。

### Design 3: Cache\-Utility Validator

第三个模块验证重构后的 prompts 是否值得采用。

由于移动 prompt block 可能影响角色遵循、工具选择和任务表现，系统会对原始 prompts 和重构后 prompts 进行轻量对比评估。若重构后的 Agent Team 在任务完成、角色遵循、工具调用、输出格式和安全约束等关键指标上没有明显退化，则接受该 prefix tree；否则回滚该重构，并将失败信息反馈给前两个模块。

## 下一步需要进行的实验

1\. 寻找合适的Agent Team / Multi\-agent例子，观察kv\-cache能够以什么样的形式共享，进行case study。

2\. 测试实际Agent Team / Multi\-agent任务中，不同agent之间最大可共享kv\-cache的比例，并验证这些可共享的kv\-cache是否以prefix形式存在？\(预期结果：可共享比例很大，但实际不是以prefix形式存在的，导致实际调用云端LLM API时，缓存命中很低\)

3\. 商业 API 的缓存机制有一些细节还要再验证下，比如：

（1）是否要求前缀达到某个最小 token 数才触发缓存？

（2）多个 Agent 请求是否真的会被服务端视为可复用的请求？（比如是不是要求一定要相同session id才行，现有的multi\-agent系统是不是用不同的session id实现的？如果是这样的话，那会不会涉及对multi\-agent系统的底层修改？但最好不要修改底层）

> （注：文档部分内容可能由 AI 生成）
