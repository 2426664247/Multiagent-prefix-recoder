.pytest_cache/
.pytest-tmp/
__pycache__/
*.py[cod]

# Local environments and secrets.
.venv/
venv/
env/
config/

# Local tool/source checkouts. Keep them available in the workspace, but do not
# treat them as project-owned source unless a task explicitly says so.
autogen/
cache_hit_proxy/

# Experiment workspaces and generated artifacts.
experiments/
tmp/
Results/
runs/
reports/
autogen_prefix_tree_results/
autogen_cache_probe/
autogen_cache_probe/results/
autogen_groupchat_kvcache_experiment/
autogen_groupchat_kvcache_experiment/raw_prompts/
autogen_groupchat_kvcache_experiment/tokenized_prompts/
autogen_groupchat_kvcache_experiment/results/
framework_cache_shapes_experiment/
framework_cache_shapes_experiment/captures/
framework_cache_shapes_experiment/tokenized_prompts/
framework_cache_shapes_experiment/results/
deepseek-cache-verifier/
deepseek-cache-verifier/results/
deepseek_agent_cache_case_study/
multiagent_benchmark_notes/

# Local prompt/candidate text artifacts generated by evaluation suites.
# Keep prompt-safe summaries/reports visible; these files can contain raw
# source prompts, candidate text, or human gold labels.
runs/**/source_prompts.jsonl
runs/**/semantic_candidates*.jsonl
runs/**/review_worklist_with_text*.jsonl
runs/**/*with_text*.jsonl
runs/**/*.local.csv
runs/**/*.local.md
runs/**/local_judge_goldset_annotation_worklist.local.jsonl
runs/**/local_judge_goldset_annotation_labels.local.jsonl
runs/**/local_judge_goldset_labeled.local.jsonl
datasets/**/local_artifacts/
datasets/**/*with_text*.jsonl
datasets/**/*.local.jsonl

# Local trace output.
cache_hit_proxy/traces/
