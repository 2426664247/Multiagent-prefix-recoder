# History / State Source

Minimal history/state smoke tasks live here. They cover history order and state
consistency checks for multi-agent prompt reordering.

The current examples are fake-smoke validation tasks, not real training labels.
