# Format / Protocol Source

Minimal format/protocol smoke tasks live here. They cover JSON schema output,
fixed fields, forbidden fields, regex validation, and strict output protocol.

The first-stage builder generates only a few smoke Utility Tasks and validates
them with the fake backend plus `JsonSchemaOracle`.
